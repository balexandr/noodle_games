# Supabase project used for the game ratings feature (see RATINGS_DESIGN.md).
# The anon key is safe to ship client-side — access is governed by Postgres
# row-level security policies on the game_ratings table, not by keeping
# this key secret.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
